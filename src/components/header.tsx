import { FC } from "react";
import { Text, TextInput, View } from "react-native";

const Header: FC = () => {
    return (
        <View>
            <Text>Animal pediA</Text>
            <TextInput />
        </View>
    );
};

export default Header;